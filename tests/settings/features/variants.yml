################################################################################
#
# Gene-level Variants Example Configuration
#
################################################################################

# General
name: variants
type: numeric
role: feature
xid: gene_id
yid: sample_id

#
# Path to CNV matrix
#
path: 'tests/data/features/variants.csv'

# Filtering
filters:
  min_reads:
    type: 'row_min_nonzero'
    value: 2
  min_var:
    type: 'row_var_above'
    value: 0

