################################################################################
#
# Gene-level Variants Example Configuration
#
################################################################################

# General
name: variants
type: variants
role: feature
xid: ensgene
yid: sample_id

#
# Path to CNV matrix
#
path: 'tests/data/features/variants.csv'

# Filters and transformations
pipeline:
  - type: 'filter_row_min_nonzero'
    value: 2
