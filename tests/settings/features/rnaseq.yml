################################################################################
#
# RNA-Seq Example Configuration
#
################################################################################

# General
name: rnaseq
type: numeric
role: feature
xid: gene_id
yid: sample_id

#
# Path to RNA-Seq count data
#
path: 'tests/data/rnaseq.csv'

# Filtering
filter:
  min_reads:
    type: 'row_sum_above'
    cutoff_value: 20
  min_var:
    type: 'row_var_above'
    cutoff_value: 0

# Transformations
transform: ['cpm', 'log2']

