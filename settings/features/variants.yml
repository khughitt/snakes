################################################################################
#
# Variant Example Configuration
#
################################################################################

# General
name: variants
type: numeric
role: feature
xid: gene_id
yid: sample_id

#
clustering:
  hclust:
    num_clusters: 100
    fxns: ['sum', 'var']
gene_sets:
  msigdb:
    fxns: ['sum', 'var']

